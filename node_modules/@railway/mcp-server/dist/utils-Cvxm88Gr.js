import { readFileSync } from "fs";
import { dirname, join } from "path";
import { fileURLToPath } from "url";

//#region src/utils.ts
const getVersion = () => {
	try {
		const packageJsonPath = join(dirname(fileURLToPath(import.meta.url)), "..", "package.json");
		return JSON.parse(readFileSync(packageJsonPath, "utf-8")).version;
	} catch (error) {
		console.error("Failed to read package version:", error);
		return "unknown";
	}
};
const createToolResponse = (text) => ({ content: [{
	type: "text",
	text
}] });

//#endregion
export { createToolResponse, getVersion };
//# sourceMappingURL=utils-Cvxm88Gr.js.map