# PPTX Engine — Skill Prompt

Place your `SKILL.md` file here. It will be used as the system prompt
when Claude generates slide plans via `POST /pptx/generate`.

If `SKILL.md` is missing, a default system prompt is used instead.
